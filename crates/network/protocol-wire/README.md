# protocol-wire

Owns the Boomerang wire contract: `ProtocolFrame`, `MessageTag`, payload/frame encoding and decoding, the tagged-wire trait surface, and the wire-only control messages used by the transport/runtime path.
