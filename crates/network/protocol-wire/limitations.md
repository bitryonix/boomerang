# protocol-wire Limitations

- The crate now owns the wire contract directly, but the tag registry is still a large hand-maintained list that should eventually be generated or validated more ergonomically.
