# boomerang-runtime Limitations

- Some role flows still rely on older state-machine helper layouts from the original PoC.
- `peer` and `niso` are now split, but some individual role modules are still large enough to justify another pass once behavior changes in those areas are needed.
